.admin-posts-wrapper
  - @posts.each do |post|
    .admin-post-box
      %ul
        %li= link_to (image_tag post.image.url(:thumb)), edit_post_path(post)
        %li
          %h1= post.title
        %li
          %p= post.description
        %li= link_to "Edit Post", edit_post_path(post)
        %li= link_to "Delete", post, method: :delete, data: { confirm: 'Are you sure?' }
