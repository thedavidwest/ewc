.posts-wrapper
  - @posts.each do |post|
    .post-box
      %ul
        %li= link_to (image_tag post.image.url(:thumb)), edit_post_path(post)
        %li
          %h1= post.title
        %li
          %p= post.description
